pipeline {
    agent any

    stages {
        stage('Deploy to Kubernetes using Helm') {
            steps {
                bat 'helm upgrade --install devops-learning-app ./helm/devops-learning-app -n dev'
            }
        }
    }
}